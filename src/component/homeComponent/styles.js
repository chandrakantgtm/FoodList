import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  btn: {
    backgroundColor: '#1c82dc',
    width: '50%',
    alignItems: 'center',
    justifyContent: 'center',
    height: 50,
    borderRadius: 4,
  },
  btnText: {
    color: '#ffffff',
    fontSize: 24,
  },
});

export default styles;
